import { createTheme } from "@mui/material";

export const greenTheme = createTheme({
  palette: {
    primary: {
      light: "",
      dark: "",
      main: "#233540",
    },
    secondary: {
      main: "#324a59",
    },
    error: {
      main: "#d9d9d9",
    },
  },
});
